import React from "react";
import ReactDOM from "react-dom/client";

import "../../../mirrorquant_demo/static/styles.css";

import App from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
